// Legacy module intentionally neutralised.
// The current Hack the Planet experience lives in /js/main.js as a harmless
// cyberpunk arcade ritual: no real targets, no packets, just movie logic.
export function createHackExperience() {
    return {
        start() {
            return 'Legacy experience retired. Boot the arcade cabinet instead.';
        },
    };
}
